# Configure NPM when running in this monorepo.
# See https://docs.npmjs.com/cli/configuring-npm/npmrc
# See https://docs.npmjs.com/cli/using-npm/config

# Don't display packages looking for funding after every `npm install`.
# But consider funding some of the amazing open source projects you depend on!
# See https://docs.npmjs.com/cli/commands/npm-fund
fund = false
